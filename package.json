{
  "name": "qnap-xml-fetcher",
  "version": "1.0.0",
  "description": "Fetch and convert QNAP XML to JSON",
  "type": "module",
  "scripts": {
    "fetch": "bun run src/fetch-xml.ts",
    "check-existing": "bun run src/check-existing-files.ts",
    "check-upload": "bun run src/check-upload.ts",
    "download": "bun run src/download.ts",
    "update": "bun run src/download-updates.ts",
    "upload": "bun run src/upload.ts",
    "force-sync": "bun run src/force-sync.ts",
    "upload-artifacts": "bun run src/upload-config-artifacts.ts"
  },
  "dependencies": {
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/xml2js": "^0.4.14"
  }
}
